export const generateReply = (tweetUrl: string): string => {
  return `There's so much to unpack about this subject! I've actually just shared some thoughts on Twitter that might add to our discussion here: ${tweetUrl}. Would love to hear your thoughts and engage further on this!
  `;
};
